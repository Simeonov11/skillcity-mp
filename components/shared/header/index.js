export default function Header( { name }){
    return(
        <div>
            <h1 className="font-bold text-4xl text-center md:text-left p-5 w-full max-w-6xl mx-auto">{name} Page</h1>
        </div>
    )
}